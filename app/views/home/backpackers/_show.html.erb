<div class="col-md-6 ">
  <div class="recom-item">
    <div class="recom-media">
    <a href="/home/backpackers/<%=combine.id%>">
        <div class="pic">
        <%if combine.cover.present?%>
        <div class="p-img" style="background-image: url(<%=project.cover%>);"></div> 
        <%else%>
        <img src="/img/800-250-2.png" alt>
        <%end%>
        </div></a>
      <div class="location"><i class="flaticon-suntour-map"></i><%=combine.the_group%></div>
    </div>
    <!-- Recomended Content-->
    <div class="recom-item-body"><a href="/home/backpackers/<%=combine.id%>">
        <h6 class="blog-title"><%=combine.name%></h6></a>
      <div class="stars stars-4"></div>
      <div class="recom-price"><span class="font-4 nt-icon"><%=combine.price4%></span>&nbsp;<%=combine.day%>天</div>
      <p class="mb-30 two-line"><%=sanitize(combine.content, :tags=>[]).first(30)%></p><a href="<%=home_backpacker_path(combine)%>" class="recom-button">閱讀更多</a><a href="#" class="cws-button small alt">馬上訂購</a>
      <div class="action font-2">20%</div>
    </div>
    <!-- Recomended Image-->
  </div>
</div>