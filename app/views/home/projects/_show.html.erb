<div class="col-md-6 ">
  <div class="recom-item">
    <div class="recom-media"><a href="/home/projects/<%=project.id%>">
        <div class="pic">
        <%if project.cover.present?%>
        <img src="<%=project.cover%>" alt>
        <%else%>
        <img src="/img/800-250-2.png" alt>
        <%end%>
        </div></a>
      <div class="location"><i class="flaticon-suntour-map"></i><%=project.the_group%></div>
    </div>
    <!-- Recomended Content-->
    <div class="recom-item-body"><a href="/home/projects/<%=project.id%>">
        <h6 class="blog-title"><%=project.name%></h6></a>
      <div class="stars stars-4"></div>
      <div class="recom-price"><span class="font-4 nt-icon"><%=project.price%></span>&nbsp;兩天三夜</div>
      <p class="mb-30 two-line"><%=project.intro.first(30)%></p><a href="<%=home_project_path(project)%>" class="recom-button">閱讀更多</a><a href="#" class="cws-button small alt">馬上訂購</a>
      <div class="action font-2"><%=project.cost%>%</div>
    </div>
    <!-- Recomended Image-->
  </div>
</div>